#!/usr/bin/env ruby -w

require_relative '../lib/skylab/git-viz/core'

_x = Skylab::GitViz::CLI.new( $stdin, $stdout, $stderr, [ $PROGRAM_NAME ] ).
  invoke ARGV

$stderr.puts "(exitstatus: #{ _x })"
