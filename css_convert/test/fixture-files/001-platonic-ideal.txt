In the folder "test/fixture-files/css"
for styles in "documentation.css" on the left, and styles in "pygments.css" on the right,
for styles under ".highlight" on the left, and styles under ".pre" on the right,
merge the styles in the left over the styles on the right where
  ".keyword"    applies to   ".k"
  ".default"    applies to   ".nc"  // name of class
  ".keyword"    applies to   ".p"   // punctuation
  ".keyword"    applies to everything else.
