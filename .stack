creative self actualization
Cover DELETE that would leave entites file empty
cover yes schema file but no ent entites dir
"business schema" that has a primitive types section
base actions perhaps not needed for that one actions
there are todo's
you still need a plugin. massive massive amounts of integration
figure out what the datamodel is. insanely what if you store the datamodel *in* the dotfile
review that one function from the service and imagine a new whitepaper
  (reminder `git --log --patch` on a whole directory (notecards or documents) stitch to already indexed)
hack a quick and dirty dotfile2sqlite
