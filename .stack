(that was J-series)
3) splice in all new content
2) will rewrite, search & remove all legacy files with `meta_attribute` in them
1) move old attributes to defined attributes
(that was N-series)
1) test-by-test, restore task examples
(that was M-series)
4) test-by-test, restore legacy (with new API)
