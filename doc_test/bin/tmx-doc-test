#!/usr/bin/env ruby -w

self._NOT_YET_not_until_016  # #while-open [#016] not a gem yet
require 'skylab/doc_test'

Skylab::Doc_test = -> i, o, e, pn_s_a, argv do
  Skylab::DocTest::CLI.new( i, o, e, pn_s_a ).invoke argv
end

if __FILE__ == $PROGRAM_NAME

  Skylab::Doc_test[ $stdin, $stdout, $stderr, [ $PROGRAM_NAME ], ARGV ]
end
