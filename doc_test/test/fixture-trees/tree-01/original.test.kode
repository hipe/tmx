module AnythingThisIsNeverLoaded

  module AsManyMoreModulesAsYouLike

    describe "mami" do

      # NOTE nothing in here ever runs.
      # the below code is all nonsense

      it "this is test D" do
        this( :test ).never changes
      end

      it "this is test B" do
        all( :of ).these lines
        will( :get ).rewritten
      end

      it "this is test E" do
        this( :test ).exists so( :that )
        we( :see ).where it( :stays )
      end
    end
  end
end
