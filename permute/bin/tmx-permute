#!/usr/bin/env ruby -w

require 'skylab/permute'

_cli = Skylab::Permute::CLI.new( nil, $stdout, $stderr, [ $PROGRAM_NAME ] )

d = _cli.invoke ARGV

if d.nonzero?
  $stderr.puts "(exitstatus: #{ d })"
end

exit d
