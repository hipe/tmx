#!/usr/bin/env ruby -w

require 'skylab/test_support'

_x = Skylab::TestSupport::CLI.new( $stdin, $stdout, $stderr, [ $PROGRAM_NAME ] ).
  invoke ARGV

$stderr.puts "(exitstatus: #{ _x })"  # covered.
