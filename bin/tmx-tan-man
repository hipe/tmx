#!/usr/bin/env ruby -w

require_relative '../lib/skylab/tan-man/core'

_x = Skylab::TanMan::CLI.new( $stdin, $stdout, $stderr, [ $PROGRAM_NAME ] ).
  invoke ARGV

$stderr.puts "(exitstatus: #{ _x })"
