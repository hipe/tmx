#!/usr/bin/env ruby

require File.expand_path('../../lib/skylab/tmx/cli', __FILE__)

Skylab::Tmx::Cli.new(
  program_name: 'tmx',
  out: $stdout,
  err: $stderr
) do
  on_info    { |e| $stderr.puts e }
  on_error   { |e| $stderr.puts e }
  on_payload { |e| $stdout.puts e }
end.run(ARGV)

