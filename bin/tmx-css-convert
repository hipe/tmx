#!/usr/bin/env ruby -w

require_relative '../lib/skylab/css-convert/core'

_x = Skylab::CSS_Convert::CLI.new( $stdin, $stdout, $stderr, [ $PROGRAM_NAME ] ).
  invoke ARGV

$stderr.puts "(exitstatus: #{ _x })"
