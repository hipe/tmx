#!/usr/bin/env ruby -w

require_relative '../lib/skylab'
require 'skylab/flex2treetop/core'

_x = Skylab::Flex2Treetop::CLI.new(
  $stdin, $stdout, $stderr, [ $PROGRAM_NAME ]
).invoke ARGV

$stderr.puts "(exitstatus: #{ _x })"
