#!/usr/bin/env ruby -w

require 'skylab/code_metrics'

_pn = ::File.basename $PROGRAM_NAME

_x = Skylab::CodeMetrics::CLI.new( $stdin, $stdout, $stderr, [ _pn ] ).
  invoke ARGV

$stderr.puts "(exitstatus: #{ _x })"
