#!/usr/bin/env ruby -w

require 'skylab/slicer'

_x = Skylab::Slicer::CLI.new(
  $stdin, $stdout, $stderr, [ ::File.basename( $PROGRAM_NAME ) ] ).
    invoke ARGV

$stderr.puts "(exitstatus: #{ _x })"
