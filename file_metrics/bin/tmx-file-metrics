#!/usr/bin/env ruby -w

require_relative '../lib/skylab/file-metrics/core'

_x = Skylab::FileMetrics::CLI.new( $stdin, $stdout, $stderr, [ $PROGRAM_NAME ] ).
  invoke ARGV

$stderr.puts "(exitstatus: #{ _x })"
