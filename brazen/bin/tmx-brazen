#!/usr/bin/env ruby -w

require 'skylab/brazen'

_x = Skylab::Brazen::CLI::Client_for_Brazen_as_Application.new(
  $stdin, $stdout, $stderr, [ $PROGRAM_NAME ] ).
    invoke ARGV

$stderr.puts "(existatus: #{ _x })"
