#!/usr/bin/env ruby -w

require_relative '../lib/skylab/treemap/core'

_x = Skylab::Treemap::CLI.new( $stdin, $stdout, $stderr, [ $PROGRAM_NAME ] ).
  invoke ARGV

$stderr.puts "(exitstatus: #{ _x })"
