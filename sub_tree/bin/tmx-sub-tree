#!/usr/bin/env ruby -w

require_relative '../lib/skylab/sub-tree/core'

_x = Skylab::SubTree::CLI.new( $stdin, $stdout, $stderr, [ $PROGRAM_NAME ] ).
  invoke ARGV

$stderr.puts "(exitstatus: #{ _x })"
