#!/usr/bin/env ruby -w

require_relative '../lib/skylab/callback/core'

_x = Skylab::Callback::CLI.new( $stdin, $stdout, $stderr, [ $PROGRAM_NAME ] ).
  invoke ARGV

$stderr.puts "(exitstatus: #{ _x })"
